SQL: select count(id) as cnt, max(id) as max_id from users;
PLAN:
RETURN RESULTS TO STORED PROCEDURE
 Serial AGGREGATION ops: SUM(VOLT_TEMP_TABLE.column#0), MAX(VOLT_TEMP_TABLE.column#1)
  RECEIVE FROM ALL PARTITIONS
   SEND PARTITION RESULTS TO COORDINATOR
    SEQUENTIAL SCAN of "USERS"
     inline Serial AGGREGATION ops: COUNT(USERS.ID), MAX(USERS.ID)

